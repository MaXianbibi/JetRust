/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{rs,html}", // Analyse uniquement les fichiers dans src
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };