use axum::{
    routing::get, // Pour définir une route qui répond à la méthode GET
    Router,      // Le composant principal d'Axum pour organiser les routes
};

use crate::handlers; 


pub fn create_routes() -> Router {
    Router::new()
 // Exemple de route
}